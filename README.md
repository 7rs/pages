# Pages

## Editor Setup

  not sure why should set following but it's necessary so write:

  ```json
  "oxc.path.oxlint": "C:/Users/[username]/scoop/shims/oxlint.exe"
  ```

  and, VSCode ain't recognize TailwindCSS, should map following:

  ```json
  "files.associations": {
    "**/tailwindcss/*.css": "tailwindcss",
    "tailwind.css": "tailwindcss",
  }
  ```
