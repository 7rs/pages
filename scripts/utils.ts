export const formatForAutocompleteLogging = (m, d?) => (d == null ? `[AutoComplete] ${m}` : `[AutoComplete] ${m}: ${d}`);
export const formatForPagefindLogging = (m, d?) => (d == null ? `[Pagefind] ${m}` : `[Pagefind] ${m}: ${d}`);
