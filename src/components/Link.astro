---
export interface Props {
  style?: string;
  href: string;
  text: string;
  title?: string;
}

const { style = "", href, text, title = href } = Astro.props;
---

<a
  class={style}
  href={href}
  title={title}
  target="_blank"
  rel="noopener noreferrer">{text}</a
>
