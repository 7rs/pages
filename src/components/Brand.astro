---
import LabeledIcon from "./LabeledIcon.astro";
---

<LabeledIcon icon="logo" description="Fast and simple website" href="/" name="brand"><slot /></LabeledIcon>


<style lang="stylus">
  @import "../styles/api.styl"

  [labeled-icon="brand"]
    gap 0.25rem
    font-size sizes.brand-size
    &:hover
      text-decoration none
    p
      deco(sizes.brand-size, sizes.brand-height)
</style>
