---
import "../styles/gradient.css";

export interface Props {
    style?: string;
    href?: string;
}

const { style = "purple", href = "" } = Astro.props;
---

<a href={href} target="_blank" rel="noopener noreferrer">
    <span class={style}>
        <slot />
    </span>
</a>
