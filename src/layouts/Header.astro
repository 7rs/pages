<header>
  <div class="header-box">
    <a class="title">Cbrnex</a>
    <nav>
      <a class="nav-item links" href="/links"></a>
      <a class="nav-item blog" href="/blog"></a>
      <a class="nav-item tech" href="https://zenn.dev/7rs"></a>
    </nav>
  </div>
</header>

<style lang="stylus">
  @import "../styles/main"

  header
    position fixed
    top 0
    left 0
    width 100%
    z-index 1

  .header-box
    @extend .static-box
    @extend .flex-row

  a
    text-decoration none
    color black
    font-size item-size
    @media (min-width wide-screen)
      font-size wide-item-size

  .title
    flex-grow 1
    flex-basis 25%

  nav
    @extend .flex-row
    @extend .flex-gap
    @media (min-width wide-screen)
      gap static-spacing

  .nav-item
    @media (max-width narrow-screen)
      background-size 100%
      height item-size
      width item-size
      &.links
        background-image url("/_astro/icons/links.svg"), url("/icons/links.svg")
      &.blog
        background-image url("/_astro/icons/blog.svg"), url("/icons/blog.svg")
      &.tech
        background-image url("/_astro/icons/tech.svg"), url("/icons/tech.svg")

    @media (min-width wide-screen)
      text-align center
      &:hover
        color red
      &.links:after
        content "Link"
      &.blog:after
        content "Blog"
      &.tech:after
        content "Tech"
</style>
